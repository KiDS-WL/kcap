
add_custom_target(planck2015
                  COMMENT "Running make for planck2015"
                  COMMAND ${CMAKE_COMMAND} -E env CC=${CMAKE_C_COMPILER} CXX=${CMAKE_CXX_COMPILER} FC=${FC} MPIFC=${MPIFC} CFITSIO_INC=${GSL_INCLUDE_DIRS} GFORTRAN_LIBPATH=${GFORTRAN_LIBPATH} CFITSIO_LIB=${GSL_LIBRARY_DIRS} LAPACK_LINK=${LAPACK_LINK} COSMOSIS_SRC_DIR=${COSMOSIS_SRC_DIR} make ${QUIET_MAKE}
                  WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
                  DEPENDS CFITSIO)
add_custom_target(planck2015-clean
                  COMMENT "Running make clean for planck2015"
                  COMMAND ${CMAKE_COMMAND} -E env CC=${CMAKE_C_COMPILER} CXX=${CMAKE_CXX_COMPILER} FC=${FC} MPIFC=${MPIFC} CFITSIO_INC=${GSL_INCLUDE_DIRS} CFITSIO_LIB=${GSL_LIBRARY_DIRS} LAPACK_LINK=${LAPACK_LINK} COSMOSIS_SRC_DIR=${COSMOSIS_SRC_DIR} make ${QUIET_MAKE} clean
                  WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})
