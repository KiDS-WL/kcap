
add_custom_target(planck2015
                  COMMENT "Running make for planck2015"
                  COMMAND ${CMAKE_COMMAND} -E env CC=${CMAKE_C_COMPILER} CXX=${CMAKE_CXX_COMPILER} FC=${FC} MPIFC=${MPIFC} CFITSIO_INC=${CFITSIO_INCLUDE_DIR} GFORTRAN_LIBPATH=${GFORTRAN_LIBPATH} CFITSIO_LIB=${CFITSIO_LIBRARY_DIR} LAPACK_LINK=${LAPACK_LINK} COSMOSIS_SRC_DIR=${COSMOSIS_SRC_DIR} make ${QUIET_MAKE}
                  WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}
                  DEPENDS CFITSIO)
add_custom_target(planck2015-clean
                  COMMENT "Running make clean for planck2015"
                  COMMAND ${CMAKE_COMMAND} -E env CC=${CMAKE_C_COMPILER} CXX=${CMAKE_CXX_COMPILER} FC=${FC} MPIFC=${MPIFC} CFITSIO_INC=${CFITSIO_INCLUDE_DIR} CFITSIO_LIB=${CFITSIO_LIBRARY_DIR} LAPACK_LINK=${LAPACK_LINK} COSMOSIS_SRC_DIR=${COSMOSIS_SRC_DIR} make ${QUIET_MAKE} clean
                  WORKING_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR})
